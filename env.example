# Copier ce fichier en .env et remplir les valeurs.
# Ne jamais commiter le fichier .env.

# === Zendesk (obligatoire) ===
ZENDESK_SUBDOMAIN=horrea
ZENDESK_EMAIL=votre-email@exemple.com
ZENDESK_API_TOKEN=votre-token-api

# === Export (défaut : CSV, pas de Google) ===
EXPORT_MODE=csv
EXPORT_OUTPUT_DIR=exports

# === Option : Google Sheets (Service Account) ===
# Chemin vers le JSON du compte de service (ou laisser vide si fichier à la racine)
# GOOGLE_SHEETS_CREDENTIALS_PATH=Pyth Apps IAM People.json
# GOOGLE_SHEET_ID=
# GOOGLE_SHEET_NAME=Tickets

# === Option : Webapp Sync vers Google Sheet (OAuth) ===
# Depuis le JSON "Client Secret OAuth" : web.client_id et web.client_secret
# Redirect URI à ajouter dans Google Cloud Console : https://VOTRE_DOMAINE/auth/google/callback
# GOOGLE_OAUTH_CLIENT_ID=xxx.apps.googleusercontent.com
# GOOGLE_OAUTH_CLIENT_SECRET=GOCSPX-xxx
# Optionnel (PythonAnywhere) : BASE_URL=https://leocarre.pythonanywhere.com

# === Application ===
# APP_HOST=0.0.0.0
# APP_PORT=8000
# DEBUG=False
